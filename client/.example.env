# Database (Supabase PostgreSQL)
DATABASE_URL=             # Supabase PostgreSQL pooler connection
DIRECT_URL=              # Supabase direct PostgreSQL connection

# Authentication
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=         # Generate with: openssl rand -base64 32
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# External Services
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PUBLISHABLE_KEY=
STRIPE_PROFESSIONAL_PRICE_ID=    # Price ID for $39 CAD plan
STRIPE_BUSINESS_PRICE_ID=        # Price ID for $59 CAD plan
NEXT_PUBLIC_STRIPE_PROFESSIONAL_PRICE_ID=  # Same as STRIPE_PROFESSIONAL_PRICE_ID (for client-side)
NEXT_PUBLIC_STRIPE_BUSINESS_PRICE_ID=      # Same as STRIPE_BUSINESS_PRICE_ID (for client-side)
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox        # sandbox/development/production
ANTHROPIC_API_KEY=       # For Claude AI

# Supabase Storage (S3-compatible)
STORAGE_ACCESS_KEY_ID=        # S3-compatible access key
STORAGE_ACCESS_KEY_SECRET=    # S3-compatible secret key
STORAGE_ENDPOINT=             # Supabase storage S3 endpoint
STORAGE_REGION=               # Storage region
STORAGE_BUCKET_NAME=          # Storage bucket name (e.g., "receipts")

# Email Service (Resend)
RESEND_API_KEY=              # Get from https://resend.com
RESEND_FROM_EMAIL=           # e.g., "SoleLedger <noreply@yourdomain.com>"